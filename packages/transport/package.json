{
  "name": "@web-midi-lab/transport",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc --ignoreConfig --pretty false --target es2020 --module esnext --moduleResolution bundler --types node --lib ES2023,DOM --skipLibCheck --outDir .tmp-test src/types.ts src/transportClock.ts src/transportClock.test.ts && node --experimental-specifier-resolution=node --test .tmp-test/transportClock.test.js && node -e \"require('node:fs').rmSync('.tmp-test', { recursive: true, force: true })\""
  },
  "main": "./src/index.ts",
  "files": [
    "src"
  ]
}
