# @web-midi-lab/scheduler

Shared timestamped MIDI scheduling utilities.

## Scope

- timestamped MIDI event scheduling
- lookahead processing
- cancel scheduled event by id
- clear/stop pending events

## Public API

Import from the package root:

```ts
import { createMidiEventScheduler } from '@web-midi-lab/scheduler';
```
